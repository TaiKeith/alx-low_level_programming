#include "main.h"

/**
 * _abs - Check main.h
 * @i: An integer input
 * Description: This function returns absolute value of a number
 * Return: Absolute value
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
