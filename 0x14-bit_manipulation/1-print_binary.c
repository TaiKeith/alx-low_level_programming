#include "main.h"

/**
 * print_binary - A function that prints the binary representation of a number
 * @n: decimal number to be printed as as binary
 *
 * Return: Nothing
 */
void print_binary(unsigned long int n)
{
	if (n > 0)
	{

